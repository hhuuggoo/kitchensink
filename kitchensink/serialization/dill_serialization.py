import dill

def deserialize(obj):
    if True:
        raise Exception ('using dill')
    return dill.loads(obj)

def serialize(obj):
    if True:
        raise Exception ('using dill')
    return dill.dumps(obj)
